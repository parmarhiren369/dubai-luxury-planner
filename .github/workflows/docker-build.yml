name: Docker Build and Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-backend:
    name: Test Backend
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package-lock.json
      
      - name: Install backend dependencies
        working-directory: ./server
        run: npm install
      
      - name: Build backend
        working-directory: ./server
        run: npm run build
      
      - name: Run backend tests (if available)
        working-directory: ./server
        run: npm test --if-present

  test-frontend:
    name: Test Frontend
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install frontend dependencies
        run: npm install
      
      - name: Build frontend
        run: npm run build
        env:
          VITE_API_URL: http://localhost:5000/api
      
      - name: Run frontend tests (if available)
        run: npm test --if-present

  build-docker:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [test-backend, test-frontend]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build backend image
        uses: docker/build-push-action@v5
        with:
          context: ./server
          push: false
          tags: wtb-backend:${{ github.sha }},wtb-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Build frontend image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: wtb-frontend:${{ github.sha }},wtb-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Test Docker Compose
        run: |
          docker-compose config
          echo "✅ Docker Compose configuration is valid"

  # Uncomment this job if you want to push to Docker Hub or GitHub Container Registry
  # push-docker:
  #   name: Push Docker Images
  #   runs-on: ubuntu-latest
  #   needs: build-docker
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #     
  #     - name: Login to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
  #     
  #     - name: Build and push backend
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: ./server
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/wtb-backend:${{ github.sha }}
  #           ${{ secrets.DOCKER_USERNAME }}/wtb-backend:latest
  #     
  #     - name: Build and push frontend
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/wtb-frontend:${{ github.sha }}
  #           ${{ secrets.DOCKER_USERNAME }}/wtb-frontend:latest
